X2--X7 authorization and retained exceptions
===========================================

On 2026-09-18 the user accepted the measured 40.60ms result and requested
starting X2--X7. This is a development-stage exception for the recorded
openloop0sched combined-process CAPTURING value of 40600240ns against its
40000000ns limit. It is not a new general limit or production P1 approval.
The original PARTIAL record, cost-protocol FAIL, incomplete later cost
states and raw artifacts remain unchanged. No attribution is admitted from
that incomplete capture, and no other safety or identity check is waived.

X1 rwsem ownership, eligible-event recall and total observer cost remain
open items. X2 may proceed without claiming those dependencies completed.
The execution order is counter updates, allocator stages, networking,
block I/O, unified routing, then joint acceptance. Unsupported E2 ownership
edges remain unknown rather than being supplied by the unfinished X1 work.

X2 begins with an independent, default-disabled page-counter source. It
records the actual native hierarchy traversal, protection propagation,
limit reversal and rollback; no update or limit semantics are changed.
An operation sample has one source timestamp and a bounded step prefix.
The timestamp is not an object lifetime generation. Native addresses with
unproven lifetime produce shared-address candidates, not proved shared
objects across calls, cache-line contention, lock holders or causal blame.
The controlled fixture uses separately declared stable objects as external
truth; that truth must not be inserted into the production explanation.
